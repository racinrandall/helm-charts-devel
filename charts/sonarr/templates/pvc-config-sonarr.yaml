---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-config-sonarr
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: nfs-sonarr-config
  storageClassName: ""
