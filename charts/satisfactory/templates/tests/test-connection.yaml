apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "satisfactory.fullname" . }}-test-connection"
  labels:
    {{- include "satisfactory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "satisfactory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
