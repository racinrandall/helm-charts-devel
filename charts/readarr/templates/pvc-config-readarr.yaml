---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-config-readarr
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: nfs-readarr-config
  storageClassName: ""
